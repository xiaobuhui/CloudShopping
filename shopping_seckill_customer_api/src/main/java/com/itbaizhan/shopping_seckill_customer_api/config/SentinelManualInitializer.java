package com.itbaizhan.shopping_seckill_customer_api.config;


/*
@Component
public class SentinelManualInitializer {

    @PostConstruct
    public void init() {
        // 1. 设置应用名称（控制台显示的名字）
        System.setProperty("project.name", "shopping_seckill_customer_api");

        // 2. 设置 Dashboard 地址（最关键！）
        System.setProperty("csp.sentinel.dashboard.server", "192.168.33.128:8899");

        // 3. 可选：手动指定客户端上报端口（如果默认8719被占用，可以自行设置）
        // System.setProperty("csp.sentinel.api.port", "8720");

        // 4. 手动触发 Sentinel 的完整初始化流程，包括 Transport 模块
        InitExecutor.doInit();

        System.out.println("✅ Sentinel 手动初始化完成，Dashboard 地址: " + System.getProperty("csp.sentinel.dashboard.server"));
    }
}*/
